---
name: code-intel
description: Code intelligence skill for pai-stack
---

# Code Intelligence Skill

This skill is parsed and served by the `pai-stack` MCP server. The operational logic, ground rules, and system prompt payloads reside in `SKILL.md`, which is mapped to `skill://code-intel` for headless agents to fetch.

## Usage

> "Use the `code-intel` skill to help me with..."

This skill is parsed and served by the `pai-stack` MCP server. The operational logic, ground rules, and system prompt payloads reside in `SKILL.md`, which is mapped to `skill://code-intel` for headless agents to fetch.
